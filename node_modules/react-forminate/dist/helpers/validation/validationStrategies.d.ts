import { ValidationResponseType, ValidationRule } from '../../types';
interface ValidationStrategy {
    validate(value: any, rule: ValidationRule): ValidationResponseType;
}
export declare class ValidationEngine {
    private strategies;
    private static instance;
    private constructor();
    private registerDefaultStrategies;
    private determineRuleType;
    registerStrategy(name: string, strategy: ValidationStrategy): void;
    validate(value: any, rules: ValidationRule[]): ValidationResponseType;
    static getInstance(): ValidationEngine;
}
export declare const validationEngine: ValidationEngine;
export {};
