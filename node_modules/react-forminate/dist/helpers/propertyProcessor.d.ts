import { ComputedValue, FieldPropContext, FormFieldType, ProcessedFieldProps } from '../types';
export declare class PropertyProcessor {
    static process<T>(prop: T | ComputedValue<T>, context: FieldPropContext): T;
    private static isComputedValue;
    static processFieldProps(field: FormFieldType, context: FieldPropContext): ProcessedFieldProps<FormFieldType>;
    private static isProcessableProperty;
}
