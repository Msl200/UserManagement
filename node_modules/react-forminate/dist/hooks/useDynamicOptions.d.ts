import { FormDataCollectionType } from '../types';
export declare const useDynamicOptions: (formSchema: FormDataCollectionType) => {
    dynamicOptions: Record<string, any[]>;
    fetchDynamicOptions: (fieldId: string, allValues?: Record<string, any>, pagination?: {
        page?: number;
        limit?: number;
    }) => Promise<void>;
};
